{
  "name": "@mu-resource/orats",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "First-party µ resource: ORATS — options_chain snapshots (strikes × expiries × greeks/IV/OI). Keyed (ORATS_API_KEY).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "mu": {
    "kind": "resource"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@mu/protocol": "workspace:*",
    "@mu/resource-sdk": "workspace:*"
  }
}
